Misc:
 + use "%if 0?%{?suse_version}" instead of "%if %suse_version"
 + PreReq: -> Requires(pre):
 + integrate spec-polish-macros into spec-cleaner

Things changed with rpm 4.7:
 + change %patch to %patch0

Things changed with rpm 4.7 that we can do only if not caring about < 11.2:
 + remove %clean section if it's just removing the build root
 + remove BuildRoot
 + use %make_install instead of %makeinstall

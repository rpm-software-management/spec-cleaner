# use docker infrastructure for faster builds
sudo: false

addons:
  apt:
    packages:
    - rpm

language: python
matrix:
  include:
    - python: 2.7
      env: TOX_ENV=py27
    - python: 3.3
      env: TOX_ENV=py33
install:
  - pip install tox
script:
  - tox -e $TOX_ENV
